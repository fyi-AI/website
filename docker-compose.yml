version: "3.8"
services:
  fyiai:
    build: .
    # Replace GHCR_OWNER with your GitHub user/org when pushing/pulling
    image: ghcr.io/fyi-ai/fyiai-sites:latest
    restart: always
    ports:
      - "80:80"
